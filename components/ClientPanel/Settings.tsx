import * as React from 'react';
import {Text, StyleSheet} from 'react-native';
import * as Login from '../ClientPanel/Selection';

export default class Settings extends React.Component {
  render() {
    return(
      

    );
  }
}